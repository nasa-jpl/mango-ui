# ex. https://localhost/mango/api
VITE_API_URL=

# ex. https://localhost/mango/docs/
VITE_MANGO_DOCS_URL=

VITE_APP_TITLE=MANGO
VITE_APP_PATH=/mango/
VITE_PROXY_API_URL=https://localhost:5173/api

# Build-time metadata surfaced on the home page. Populated by CI at Docker
# build time (see .github/workflows/build.yml). Optional in local dev — leave
# unset to render "—" placeholders, or set them here to see them in `npm run dev`.
# VITE_APP_RELEASE=
# VITE_GIT_COMMIT_HASH=
